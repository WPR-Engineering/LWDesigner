%p
#notice= notice

%p
  %h1 Hostname: #{ @node_gpio.hostname }

%p
  %b Location:
  = @node_gpio.location
%p
  %b IP Address:
  = @node_gpio.ipAddress


%h3 Terminals
%hr
#ins
%table{:class => "table"}
  %thead{:style => "text-align: left"}
    %tr
      %th Name on Network
      %th GPIO Pin
      %th Description
      %th Livewire Channel
      %th Notes


  %tbody
    - @node_gpio.gpio_terminals.each do |terminals|
      %tr
        %td=terminals.name
        %td=terminals.pin
        %td=terminals.description
        %td=terminals.lwchannel
        %td=terminals.notes


= link_to 'Edit', edit_node_gpio_path(@node_gpio), :class => "btn btn--s btn--gray"
\|
= link_to 'Back', node_gpios_path, :class => "btn btn--s btn--gray"
%h2 Change History
%hr
%p note: only the 5 most recent changes are displayed on this page.
%p= link_to 'Full Change History', full_history_node_gpio_path, :class => "btn btn--s btn--gray"
#audited
=render 'history'
